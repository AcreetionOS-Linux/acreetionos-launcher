plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace 'org.acreetionos.launcher.compatlib.fourteen'
}

addFrameworkJar('framework-14.jar')

dependencies {
    api projects.compatLib.compatLibVT
}
