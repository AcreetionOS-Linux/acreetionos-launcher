plugins {
    id 'com.android.library'
}

android {
    namespace 'org.acreetionos.launcher.compatlib.sixteen'
}

addFrameworkJar('framework-16.jar')

dependencies {
    api projects.compatLib.compatLibVV
}
