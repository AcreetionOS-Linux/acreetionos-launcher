plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace "org.acreetionos.launcher.compatlib.ten"
}

addFrameworkJar('framework-10.jar')

dependencies {
    api projects.compatLib
}
